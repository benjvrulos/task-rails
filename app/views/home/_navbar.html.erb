
<nav class="navbar navbar-expand-lg bg-dark bg-body-tertiary" data-bs-theme="dark">
    <div class="container-fluid">

        <%= link_to "Task App", root_path, class:"navbar-brand"%>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to "Proyectos", projects_path, class:"nav-link"%>
                </li>
                <li class="nav-item">
                    <%= link_to "Nuevo Proyecto", new_project_path, class:"nav-link"%>
                </li>
            
            </ul>
            <% if user_signed_in? %>
                <div class="d-flex gap-3 align-items-center">
                    <p class="welcome-username">Bienvenido <%= current_user.email %></p>
                    <%= button_to "Salir", destroy_user_session_path, method:"delete", class:"btn btn-danger px-3" %>
                </div>
            <% end%>
            
        </div>
    </div>
</nav>